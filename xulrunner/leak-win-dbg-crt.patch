--- a/dbm/src/hash.c	2014-06-26 12:33:49 -0500
+++ a/dbm/src/hash.c	2014-06-25 16:59:23 -0500
@@ -485,7 +485,10 @@
 		if (hashp->is_temp)
 			(void)unlink(hashp->filename);
 #endif
+	/* Leak this memory to prevent CRT assertion being thrown */
+#if !(defined(_WIN32) && defined(DEBUG))
 		free(hashp->filename);
+#endif
 	}
 	if (hashp->tmp_buf)
 	    free(hashp->tmp_buf);
